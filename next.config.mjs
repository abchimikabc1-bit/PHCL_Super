/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export', // disabled for dev to allow API routes

  // output: 'export', // disabled for dev to allow API routes
  // Usanidi sahihi unaokubalika na Next.js na Firebase
};

export default nextConfig;
